- platform: template
  covers:
    outside_gate:
      friendly_name: "Main Gate"
      open_cover:
        service: switch.turn_on
        data:
          entity_id: switch.outside_gate
      close_cover:
        service: switch.turn_on
        data:
          entity_id: switch.outside_gate
